//const height = 100 // footer 
export const mainMixin = {
    data() {
        return {

        }
    },
    mounted() {
        // con 高度自适应
        // const el = this.$el
        // if (!el || !el.querySelector) return
        // const ref = el.querySelector('.con')
        // if (!ref) return
        // const windowHeight = window.document.documentElement.clientHeight
        // ref.style.height = windowHeight - (ref.offsetTop - el.offsetTop) - height + "px"
    },
    methods: {}
}
